spring:
  jpa:
    hibernate:
      #      ddl-auto: none
      ddl-auto: create

    properties:
      hibernate:

    show_sql: true
    format_sql: true
    #        default_batch_fetch_size: 100
    database: h2
  logging.level:
    org.hibernate.SQL: debug

